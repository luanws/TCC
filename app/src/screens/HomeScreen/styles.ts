import { ScrollView, View } from "react-native"
import { styled } from "../../hooks/theme"

export const Scroll = styled(ScrollView)`
`

export const CardsContainer = styled(View)`
    padding: 8px 16px;
    padding-bottom: 0px;
`

export const NavigateButtonsContainer = styled(View)`
    flex-direction: row;
    flex-wrap: wrap;
    padding: 0px 12px;
`